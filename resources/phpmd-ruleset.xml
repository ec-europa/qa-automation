<?xml version="1.0"?>
<ruleset name="OpenEuropa PHPMD rule set"
         xmlns="http://pmd.sf.net/ruleset/1.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sf.net/ruleset/1.0.0 http://pmd.sf.net/ruleset_xml_schema.xsd"
         xsi:noNamespaceSchemaLocation="http://pmd.sf.net/ruleset_xml_schema.xsd">
    <description>Custom PHP Mess Detector ruleset for OpenEuropa projects</description>

    <!-- Import entire naming rule set and exclude rules that don't apply -->
    <rule ref="rulesets/naming.xml">
        <exclude name="ShortVariable" />
        <exclude name="LongVariable" />
        <exclude name="ConstructorWithNameAsEnclosingClass" />
        <exclude name="LongClassName" />
    </rule>

    <!-- Import entire codesize rule set and exclude rules that don't apply -->
    <rule ref="rulesets/codesize.xml">
        <exclude name="ExcessiveMethodLength" />
        <exclude name="ExcessiveClassLength" />
        <exclude name="ExcessivePublicCount" />
        <exclude name="TooManyMethods" />
        <exclude name="TooManyPublicMethods" />
    </rule>
</ruleset>
